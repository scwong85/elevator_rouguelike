# Elevator Roguelike (Web)

Python/Flask rogue-lite narrative elevator game for mobile portrait.

## Run locally

```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python app.py
